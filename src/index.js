import React from 'react' // Initial Pull-Up
import ReactDOM from 'react-dom' // Initila Set-up
import App from './App' // This is App Js
import './index.css' // This is styling

ReactDOM.render(<App />, document.getElementById('root')) // This is fuck knows what?